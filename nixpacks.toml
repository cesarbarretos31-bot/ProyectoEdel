[phases.setup]
# Instala PHP 8.2 + Composer + extensión intl
nixPkgs = ["php82", "php82Packages.composer", "php82Packages.php-intl"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
